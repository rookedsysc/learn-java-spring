spring:
  jpa:
    show-sql: true
    properties:
      format_sql: true
      dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: validate
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
  datasource:
    # jdbc:mysql://localhost:3306/[TABLE NAME]?userSSL=false&useUnicode=true&allowPublicKeyRetrieval=true
    url: jdbc:mysql://192.168.1.2:3306/jpa_json_performance_test?userSSL=false&useUnicode=true&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: df159357
#    hikari:
#      minimum-idle: 20 # ?? ?? ?? ??
#      maximum-pool-size: 20 # ?? ? ??
#      idle-timeout: 30000
#      max-lifetime: 1800000
#      connection-timeout: 30000